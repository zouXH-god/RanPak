# Event Finder UI (Vue + Router + Element Plus)

- 已集成 Vue Router：/、/inbox、/invites、/standups、/calendar、/settings
- 已集成 Element Plus，并使用 CSS 变量自定义主题色与 Tailwind 配色一致
- 运行：`npm i && npm run dev`（或 pnpm/yarn）
